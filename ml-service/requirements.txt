fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.10.1

# httpx<0.28 required — openai passes 'proxies' to httpx.Client, removed in 0.28+
httpx==0.27.2

langchain-core==0.3.19
langchain-openai==0.2.1
openai==1.54.0

# Azure Service Bus — reads task queue and calls back to Java after enrichment
azure-servicebus==7.12.0
requests==2.32.3

# Azure Key Vault — fetches secrets at startup via managed identity (prod) or az login (local)
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0
